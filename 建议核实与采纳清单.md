# ArtDuo 建议核实与采纳清单（V1）

> 依据：`1ArtDuo-画廊提升计划.md`；`1策展反馈.md`未找到（留出占位，待补充合并）。

## 一、可直接采纳（短期内可落地）

- **修复构建配置**：移除 `next.config.ts` 中忽略 TS/ESLint 错误的设置，恢复严格校验。
- **搭建测试框架**：引入 Jest + Testing Library；端到端建议 Cypress，覆盖核心组件如 `ArtworkCard`。
- **图片优化策略**：使用 Next Image，开启占位、按需大小；逐步引入 WebP/AVIF；设定合理 `sizes`。
- **可访问性基础修复**：统一焦点可见样式、语义标签与 ARIA；键盘可达性检查。
- **Web Vitals 监控**：埋点 LCP/FID/CLS/FCP，控制台与上报管道预留接口。

## 二、建议采纳（需要少量设计/开发配合）

- **情绪化动态主题系统**：按 `lonely/passion/joy` 先做 3 套主题 Token，渐进增强背景与粒子效果（可开关）。
- **沉浸式动画系统**：建立集中动画配置（hover、spotlight、breath），组件按需接入，限制动画时长与次数以控性能。
- **画廊布局升级**：
  - 智能瀑布流：先用纯 CSS columns 方案，后续再评估 Masonry 虚拟化。
  - 虚拟滚动：长列表使用 `react-window`/`react-virtualized`，图片懒加载配合占位。

## 三、需评估后推进（中期投入）

- **高级搜索与策展**：后端索引与筛选维度定义（情绪、风格、时期、媒介、博物馆、色彩）；需要 API 设计与数据源支持。
- **个性化推荐**：先做基于偏好的轻量规则模型，收集交互数据后再引入模型化排序。
- **移动端深度优化**：
  - 自定义响应式 Hook 与断点策略；
  - 手势库选型（如 `react-swipeable`），与现有交互冲突评估。
- **设计令牌体系**：颜色/间距/排版 Token 化，驱动 Tailwind/样式变量统一输出；需与设计侧对齐命名与层次。

## 四、不建议直接采纳或需谨慎（理由）

- **过度动画与粒子效果**：在低端设备上影响流畅度与电量，建议设“低性能模式”开关并降级。
- **一次性目标值承诺**（如测试覆盖率到 80%、LCP 到 1.5s）：应分阶段看板化推进，避免短期内影响交付节奏。

## 五、排期与优先级（建议）

- **P0（本周）**：构建配置修复、Web Vitals 监控、图片优化初版、A11y 基线、测试框架初始化。
- **P1（2-3 周）**：动画配置中心、情绪主题 MVP、瀑布流与虚拟滚动、关键路径组件单测覆盖。
- **P2（4-6 周）**：高级搜索 MVP、个性化推荐雏形、移动端手势与响应式增强、设计令牌输出。

## 六、落地清单（DoD）

- 构建无 TS/ESLint 警告；CI 阶段强校验。
- Web Vitals 指标可上报并在日志可见。
- 图片使用 Next/Image 且具备占位与自适应尺寸。
- `ArtworkCard` 等关键组件具备基础单测。
- 键盘可达、焦点可见、语义化结构通过快速审查。

## 七、待合并占位（来自 `1策展反馈.md`）

- 文件缺失：待提供后，将在以下分区补充“采纳/待评估/不采纳+理由”。
  - 采纳：
  - 待评估：
  - 不采纳：

—
文档维护者：产品/前端协作小组
更新时间：以最后一次合并为准
