# ArtDuo 改进提案（合并诊断 + 提示词优化 + 流式输出方案）

## 1. 现状与关键问题
- 粗筛抓取正常，但评分/精选链路偶发为 0，日志难以快速定位（见综合测试报告与《提升文档》）
- `serviceInfo` 与真实数据源不一致；`successfulServices` 为空未兜底
- 讲解/总结偶发模板化，受 LLM 超时与解析失败影响；密钥加载不稳定
- 存在冗余 LLM 调用；缺乏“离线/无网络”明确策略，降级时耗时偏长

## 2. 必要修复（高优先级）
- 评分阶段兜底：在 `batchJudgeArtworksUltraOptimized` 内对 `preFilteredArtworks.length` 与 `finalScores.length` 进行强校验，为 0 则抛错，触发外层 fallback；fallback 评分应返回最少 25 条默认分
- 服务成功准则统一：当 `artworks.length === 0`，各 API 服务返回 `success: false`，让管理器走降级；`successfulServices` 为空时，`currentServiceIndex` 与状态上报必须兜底为 `fallback`
- 监控增强：在 diagnostics 中统一输出 `coarseCount`、`preFilterCount`、`scoredCount`、`selectedCount`
- 去冗余：路由二次 LLM 调用合并/删除，只保留 `generateCurationSummary`

## 3. 提示词优化（按《提示词提升》统一 Schema）
- 关键词生成：强制 JSON schema `{ keywords: string[], fallback: boolean }`，仅英文，长度 5，不要 Markdown
- 批量评分：统一 `{ scores: [{ artwork_id, emotion_fit, artistic_value, visual_impact, overall_recommendation, confidence, reasoning }], errors: string[] }`；范围与精度严格约束
- 单件讲解：中文输出，五段各 80–120 字；`confidence` 为 0–1 两位小数；仅 JSON，无 Markdown
- 策展总结：220–260 字三段式纯文本，不含标题；作品数为 0 时返回引导语
- 前端 Agent 与服务端共用模板，保证容错与缓存一致

## 4. 流式输出 + 后台加载方案

### 4.1 流式输出目标
- 用户先快速看到“结构化骨架”（主题/关键词/候选数量/情绪曲线预览）
- 评分、讲解、总结分阶段流式推送（Server-Sent Events 或 chunked streaming）
- 页面以增量方式渲染：先展缩略卡，再逐条补全分数与讲解

### 4.2 技术实施（Next.js App Router）
- API 层：
  - 新增 `GET /api/curate/stream`（SSE）或 `POST /api/curate` 返回 ReadableStream，将流程分段 emit：
    - step:plan → 关键词与过滤器
    - step:coarse → 候选作品列表（去重后）
    - step:score → 批次评分结果（每批 emit）
    - step:select → 最终精选 9 件
    - step:explain → 讲解批次（每 3 条 emit）
    - step:summary → 策展总结
- 客户端：
  - `useEffect` 订阅 SSE：维护 `curationState`（plan、coarse、scores、selected、explanations、summary）
  - 渲染策略：
    - 立即渲染 plan 与 coarse（骨架屏）
    - scores 到达后更新卡片评分；selected 到达后锁定 9 件
    - explanations 按到达顺序补齐文案；summary 最后替换顶部描述

### 4.3 后台加载与缓存
- IndexedDB/Memory 缓存批量评分与讲解，key 由 `artworkId+emotion+userInputHash` 组成，TTL 24h
- 重复请求命中缓存则跳过对应批次，SSE 仍发送已命中事件（带 `fromCache: true`）
- 离线模式：配置 `OFFLINE_MODE=true` 时直接走规则评分 + 模板讲解，1–2 秒返回完整结果

### 4.4 超时与降级
- 评分批次 30s 超时，超时即回填默认分并标注 `degraded: true`
- 讲解调用 30s 超时，fallback OpenAI → 模板；SSE 事件带 `degraded: true`，前端标注“简版讲解”

## 5. 环境变量与密钥注入建议
- 本地：优先用“内联变量启动”确保进程环境带上密钥
  - `GLM_API_KEY=xxx NEXT_PUBLIC_GLM_API_KEY=xxx npm run dev`
- 部署：在平台环境变量面板同时设置服务端与 NEXT_PUBLIC 前缀，以便服务端和浏览器端都可用
- 代码端兜底：GLM 客户端在检测不到密钥时直接抛错并在 diagnostics 标注 `glmKey: false`

## 6. 里程碑与验收
- M1（当天）：评分兜底 + 服务成功准则 + 诊断字段 + 去冗余调用
- M2（+1 天）：提示词统一 Schema（关键词/评分/讲解/总结）
- M3（+2 天）：SSE 流式接口 + 前端订阅与增量渲染
- M4（+3 天）：缓存（IndexedDB）+ 离线模式 + 端到端测试报告

## 7. 预期收益
- 可观测性提升：三处计数让问题定位从分钟级降至秒级
- 体验优化：用户 1–2 秒看到骨架，讲解与总结逐步补全
- 稳定性提升：评分与讲解不再“静默为 0”，全部有兜底
- 成本可控：去冗余调用 + 缓存与离线模式，LLM 成本显著下降
