{
  "name": "@artduo/corpus",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@artduo/contracts": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @artduo/contracts build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @artduo/contracts build && tsx --test src/*.test.ts"
  }
}
