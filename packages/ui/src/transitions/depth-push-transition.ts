import type { TransitionModule } from "./registry";

export const depthPushTransition: TransitionModule = {
  family: "depth-push",
  className: "immersive-transition-depth-push",
  durationMs: 820,
};
