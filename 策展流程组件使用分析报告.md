# ArtDuo 策展流程组件使用情况分析报告

生成时间: 2025-10-27

## 📋 执行摘要

经过全面检查，当前系统真正使用的策展流程非常清晰，主要集中在 `/api/curate/stream` 路由。大量历史遗留文件已经不再使用，建议清理以提高代码可维护性。

---

## ✅ 真正使用的组件

### 1. 主策展流程 (`/api/curate/stream/route.ts`)

这是当前**唯一被前端调用的策展API**，画廊页面 `page.html` 通过 `fetch('/api/curate/stream')` 调用此接口。

**流程步骤：**

#### Phase A: 规划层（后端角度）

1. **LLM策展规划** 
   - 文件: `real-llm-curation-intent-fixed.ts`
   - 功能: 生成策展主题、情绪弧线、情绪阶段
   - 依赖: `curation-intent-schema.ts` (JSON Schema验证)

2. **情绪曲线设计**
   - 文件: `emotion-curve.ts`
   - 功能: 基于LLM生成的情绪阶段信息生成情绪曲线

3. **向量检索**
   - 文件: `js-vector-search.ts`
   - 功能: 纯JavaScript实现的向量搜索，避免native模块依赖
   - 特点: 使用余弦相似度计算，包含模拟数据

4. **作品选择**
   - 位置: `stream/route.ts` 内部函数 `selectFinalArtworks()`
   - 功能: 从搜索结果中选择6-12件最终作品

#### Phase B: 叙述层（SSE流式输出）

5. **序言+结语生成**
   - 位置: `stream/route.ts` 内部函数 `generateNarration()`
   - 依赖: `glm-optimized-client.ts`

6. **作品讲解批次生成**
   - 文件: `artwork-explanation.ts`
   - 功能: 分批并发生成作品讲解（2-2-2模式）
   - 依赖: `artwork-explanation-schema.ts` (JSON Schema验证)
   - LLM客户端: `glm-optimized-client.ts` (主), `openai.ts` (备用)

### 2. LLM客户端

#### GLM客户端 (`glm-optimized-client.ts`)
- **主要客户端**，所有LLM调用优先使用
- 支持快速模式(quickChat)和深度分析模式(deepAnalysis)
- 支持thinking参数控制

#### OpenAI客户端 (`openai.ts`)
- **备用客户端**，当GLM不可用时使用
- 在 `artwork-explanation.ts` 中作为fallback

### 3. 类型定义

- `types.ts` - 定义了 `Artwork`, `ArtworkScore` 等核心类型

### 4. 辅助组件

- `config-manager.ts` - 配置管理（可能被使用）

---

## ❌ 未使用的组件（建议清理）

### 策展相关文件（`src/lib/curation/`）

以下文件**不在**当前主流程中使用：

#### 1. 旧版LLM评分/判断系统
- ❌ `llm-judge.ts`
- ❌ `llm-judge-enhanced.ts`
- ❌ `llm-judge-optimized.ts`
- ❌ `llm-judge-ultra-optimized.ts`
- **状态**: 被 `stream/route.ts` 的内部评分逻辑替代

#### 2. 旧版搜索计划构建器
- ❌ `search-plan-builder.ts`
- ❌ `search-plan-builder-optimized.ts`
- ❌ `search-plan-ultra-optimized.ts`
- **状态**: 被 `stream/route.ts` 的内部 `generateSearchQueries()` 替代

#### 3. 旧版策展意图生成器
- ❌ `llm-curation-intent.ts`
- ❌ `real-llm-curation-intent.ts` (被fixed版本替代)
- ❌ `real-llm-curation-intent-backup.ts` (备份文件)
- **状态**: 统一使用 `real-llm-curation-intent-fixed.ts`

#### 4. 旧版策展策略
- ❌ `enhanced-curation-strategy.ts`
- ❌ `intelligent-curation-strategy.ts`
- ❌ `optimized-curation-flow.ts`
- ❌ `llm-guided-curation.ts`
- ❌ `unified-curation-generator.ts`
- **状态**: 这些策略文件已被 `stream/route.ts` 的简化流程替代

#### 5. 旧版讲解生成器
- ❌ `llm-artwork-explanation.ts`
- **状态**: 被 `artwork-explanation.ts` 替代

#### 6. 旧版情绪曲线生成器
- ❌ `llm-emotion-curve.ts`
- **状态**: 被 `emotion-curve.ts` 统一处理

#### 7. 旧版作品选择器
- ❌ `artwork-selector.ts`
- **状态**: 被 `stream/route.ts` 的内部选择逻辑替代

### API路由文件（`src/app/api/curate/`）

#### 旧版主路由
- ❌ `route.ts` - 使用了许多已废弃的组件（如 `llm-judge-ultra-optimized`, `artwork-selector`）
- ❌ `stream/route-original.ts` - 原始版本，已被新版替代

#### 旧版LLM流式路由
- ❌ `llm-stream/route.ts` - 使用了 `unified-curation-generator`, `llm-artwork-explanation` 等废弃组件

#### 健康检查/诊断路由（可选保留）
这些路由可能用于监控和调试，是否保留取决于实际需求：
- 🤔 `health/route.ts`
- 🤔 `emotion-curve-health/route.ts`
- 🤔 `llm-scoring-health/route.ts`
- 🤔 `cache-status/route.ts`
- 🤔 `cache-health/route.ts`
- 🤔 `cache-cleanup/route.ts`
- 🤔 `cache-clear/route.ts`

#### 其他测试路由
- ❌ `explain/route.ts` - 独立讲解接口（可能用于测试）

### 向量搜索相关（`src/lib/vector-search/`）

当前使用 `js-vector-search.ts`，以下文件可能未使用：
- 🤔 `vector-index.ts`
- 🤔 `hybrid-search.ts`
- 🤔 `emotion-classifier.ts`
- 🤔 `data-quality-weights.ts`

**建议**: 需要进一步检查这些文件的引用情况

---

## 📊 当前架构图

```
前端 (gallery/immersive/page.html)
  ↓
  fetch('/api/curate/stream')
  ↓
策展流式API (stream/route.ts)
  ├─ Phase A: 规划层
  │   ├─ realLLMCurationIntentGenerator (real-llm-curation-intent-fixed.ts)
  │   │   ├─ validateCurationIntent (curation-intent-schema.ts)
  │   │   └─ glmOptimizedClient
  │   ├─ EmotionCurveGenerator (emotion-curve.ts)
  │   ├─ jsVectorSearchService (js-vector-search.ts)
  │   └─ selectFinalArtworks() [内部函数]
  └─ Phase B: 叙述层
      ├─ generateNarration() [内部函数]
      │   └─ glmOptimizedClient
      └─ generateArtworkExplanations (artwork-explanation.ts)
          ├─ validateArtworkExplanation (artwork-explanation-schema.ts)
          ├─ glmOptimizedClient [主]
          └─ openaiClient [备用]
```

---

## 💡 优化建议

### 1. 立即清理（高优先级）

**删除以下肯定不使用的文件:**

```bash
# 策展文件
rm frontend/src/lib/curation/llm-judge*.ts
rm frontend/src/lib/curation/search-plan-builder*.ts
rm frontend/src/lib/curation/llm-curation-intent.ts
rm frontend/src/lib/curation/real-llm-curation-intent.ts
rm frontend/src/lib/curation/real-llm-curation-intent-backup.ts
rm frontend/src/lib/curation/enhanced-curation-strategy.ts
rm frontend/src/lib/curation/intelligent-curation-strategy.ts
rm frontend/src/lib/curation/optimized-curation-flow.ts
rm frontend/src/lib/curation/llm-guided-curation.ts
rm frontend/src/lib/curation/unified-curation-generator.ts
rm frontend/src/lib/curation/llm-artwork-explanation.ts
rm frontend/src/lib/curation/llm-emotion-curve.ts
rm frontend/src/lib/curation/artwork-selector.ts

# API路由
rm frontend/src/app/api/curate/route.ts
rm frontend/src/app/api/curate/stream/route-original.ts
rm -rf frontend/src/app/api/curate/llm-stream/

# 测试路由（根据需要决定是否删除）
rm frontend/src/app/api/curate/explain/route.ts
```

**预期收益:**
- 减少约15个文件
- 减少约5000-8000行代码
- 降低维护成本
- 避免未来的混淆

### 2. 条件清理（中优先级）

**检查后删除健康检查路由:**

如果不需要监控功能，可以删除：
```bash
rm -rf frontend/src/app/api/curate/health/
rm -rf frontend/src/app/api/curate/emotion-curve-health/
rm -rf frontend/src/app/api/curate/llm-scoring-health/
rm -rf frontend/src/app/api/curate/cache-*/
```

### 3. 进一步检查（低优先级）

**向量搜索组件:**
- 检查 `vector-index.ts`, `hybrid-search.ts` 等是否被使用
- 如果不使用，考虑删除

**LLM客户端整合:**
- 检查 `frontend-llm-client.ts` 和 `unified-llm-client.ts` 是否被使用
- 如果不使用，只保留 `glm-optimized-client.ts` 和 `openai.ts`

### 4. 文档更新

更新 README.md，清晰说明：
- 当前使用的API端点只有 `/api/curate/stream`
- 核心组件清单
- 策展流程说明

---

## 📝 清理步骤建议

### 步骤1: 备份
```bash
git checkout -b cleanup-unused-components
git add -A
git commit -m "backup: before cleanup"
```

### 步骤2: 删除文件
```bash
# 执行上述删除命令
```

### 步骤3: 测试
```bash
cd frontend
npm run build
npm run dev
# 访问画廊页面，测试策展功能
```

### 步骤4: 提交
```bash
git add -A
git commit -m "cleanup: remove unused curation components

- Removed old LLM judge/scoring systems
- Removed old search plan builders
- Removed old curation intent generators
- Removed old curation strategies
- Removed old API routes
- Current system only uses /api/curate/stream
"
```

---

## ⚠️ 注意事项

1. **测试路由**: 如果需要保留测试功能，可以保留 `explain/route.ts` 和健康检查路由
2. **监控需求**: 如果需要系统监控，保留 health 和 cache 相关路由
3. **向量搜索**: `js-vector-search.ts` 当前使用模拟数据，可能需要进一步完善
4. **备份重要**: 删除前务必做好备份，确保可以回滚

---

## 📈 清理后的收益

| 指标 | 清理前 | 清理后 | 改善 |
|------|--------|--------|------|
| 策展相关文件数 | ~23个 | ~8个 | -65% |
| 代码行数（估算） | ~12,000行 | ~6,000行 | -50% |
| API路由数 | ~15个 | ~1-2个 | -85% |
| 维护复杂度 | 高 | 低 | 显著降低 |
| 新人理解难度 | 困难 | 简单 | 显著降低 |

---

## 🎯 结论

当前系统的策展流程已经非常精简和高效，集中在 `/api/curate/stream` 一个端点。大量历史遗留的组件和路由已经不再使用，建议按照上述计划进行清理，以提高代码质量和可维护性。

**核心组件（保留）:**
- `stream/route.ts` - 主策展流程
- `real-llm-curation-intent-fixed.ts` - LLM策展意图生成
- `emotion-curve.ts` - 情绪曲线生成
- `js-vector-search.ts` - 向量搜索
- `artwork-explanation.ts` - 作品讲解生成
- `glm-optimized-client.ts` - GLM客户端
- `openai.ts` - OpenAI备用客户端
- 两个schema文件用于验证

**清理文件（删除）:**
- 所有旧版judge/scoring系统
- 所有旧版search-plan-builder
- 所有旧版curation-intent生成器（除fixed版本）
- 所有旧版策展策略文件
- 所有旧版API路由（除stream外）

