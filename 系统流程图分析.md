# ArtDuo 系统流程分析

## 整体架构概览

ArtDuo 是一个基于 Next.js 的 AI 驱动艺术策展平台，采用纯前端架构，通过 API 路由处理策展逻辑。

## 核心流程分析

### 1. 用户交互流程

```
用户输入情绪 → 主页 (page.html) → 沉浸式画廊 (immersive/page.html)
     ↓
情绪关键词输入 → 跳转到沉浸式页面 → 后台策展流程
```

### 2. 策展核心流程

```
用户情绪输入
    ↓
搜索计划构建 (search-plan-ultra-optimized.ts)
    ↓
多服务并发搜索 (ArtworkServiceManager)
    ├── Met Museum MCP
    ├── Met Museum API  
    ├── Rijks Museum API
    └── Fallback Service
    ↓
LLM评分系统 (llm-judge-ultra-optimized.ts)
    ├── 智能预筛选
    ├── 批量评分 (GLM批处理API)
    └── 评分缓存
    ↓
情绪曲线生成 (emotion-curve.ts)
    ├── 线性/波浪/峰值/谷值曲线
    └── 自定义曲线 (基于评分)
    ↓
智能作品选择 (artwork-selector.ts)
    ├── 按评分选择
    ├── 按多样性选择
    ├── 按情绪曲线选择
    └── 平衡选择策略
    ↓
作品讲解生成 (artwork-explanation.ts)
    ├── 情绪关联分析
    ├── 艺术分析
    ├── 历史背景
    ├── 策展理由
    └── 用户相关性
    ↓
策展总结生成
    ↓
返回完整策展结果
```

### 3. 数据流架构

```
前端状态管理 (Zustand Store)
    ├── 用户输入状态
    ├── 策展结果状态
    ├── 展览状态
    ├── UI状态
    ├── 对话状态
    └── 主题状态
    ↓
API路由处理 (/api/curate/route.ts)
    ├── 搜索计划构建
    ├── 多服务并发搜索
    ├── LLM评分
    ├── 情绪曲线生成
    ├── 作品选择
    ├── 讲解生成
    └── 结果返回
    ↓
沉浸式画廊展示
    ├── 前言页面
    ├── 艺术作品轮播
    └── 结语页面
```

### 4. 技术栈分析

#### 前端技术
- **框架**: Next.js 15.5.3 (App Router)
- **UI库**: React 19.1.0 + TypeScript
- **状态管理**: Zustand + localStorage持久化
- **样式**: Tailwind CSS 4.0
- **动画**: Framer Motion + GSAP
- **字体**: Inter + Playfair Display + Noto Sans SC

#### 后端技术
- **API**: Next.js API Routes
- **LLM集成**: 智谱AI GLM-4.5 + OpenAI
- **数据源**: Met Museum API + Rijks Museum API + MCP
- **缓存**: 内存缓存 + IndexedDB

#### 核心服务
- **策展服务**: 多级服务管理器
- **评分系统**: 超优化批量评分
- **情绪曲线**: 智能曲线生成器
- **作品选择**: 多策略选择器
- **讲解生成**: 批量讲解系统

### 5. 性能优化策略

#### 搜索优化
- 智能预筛选减少评分工作量
- 优先选择painting类作品
- 关键词精确匹配

#### 评分优化
- 批量处理减少API调用
- 智能缓存机制
- 降级策略保证可用性

#### 讲解优化
- 并发处理提升速度
- 指数退避重试机制
- 缓存命中优化

#### 前端优化
- 图片预加载策略
- 轮播动画优化
- 响应式设计

### 6. 错误处理机制

#### 服务降级
```
主要服务失败 → 降级服务 → 本地数据
```

#### 评分降级
```
批处理失败 → 传统方法 → 默认评分
```

#### 讲解降级
```
LLM失败 → 降级讲解 → 基础模板
```

### 7. 缓存策略

#### 评分缓存
- 24小时TTL
- 基于作品ID+情绪+用户输入
- 内存存储

#### 讲解缓存
- 字段级缓存
- 哈希键生成
- 过期清理

#### 前端缓存
- localStorage持久化
- 主题设置缓存
- 访问历史记录

## 系统优势

1. **纯前端架构**: 零服务器成本，Vercel免费托管
2. **智能策展**: 多维度评分和选择策略
3. **情绪曲线**: 科学的情感编排
4. **性能优化**: 批量处理和缓存机制
5. **用户体验**: 沉浸式画廊展示
6. **扩展性**: 模块化设计，易于维护

## 改进建议

1. **监控系统**: 添加性能监控和错误追踪
2. **A/B测试**: 不同策展策略的效果对比
3. **用户反馈**: 收集用户偏好优化算法
4. **数据可视化**: 策展过程的可视化展示
5. **国际化**: 多语言支持
6. **移动端优化**: PWA功能增强
